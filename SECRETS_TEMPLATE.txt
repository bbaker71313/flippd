# ============================================================
# FLIPPD BACKEND — REPLIT SECRETS
# ============================================================
# Copy each KEY=VALUE pair into Replit Secrets (🔒 lock icon)
# Add them ONE AT A TIME — Replit needs Key + Value separately
# ============================================================

# Anthropic (AI scans) — get from console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-REPLACE_WITH_YOUR_KEY

# Resend (transactional email) — get from resend.com/api-keys
RESEND_API_KEY=re_REPLACE_WITH_YOUR_KEY

# Stripe (subscriptions) — get from dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_KEY

# Stripe Webhook Secret (added AFTER you set up webhook in Stripe Dashboard)
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_AFTER_WEBHOOK_SETUP

# Stripe Price IDs — get from dashboard.stripe.com/products
STRIPE_PRICE_HUSTLE_MONTHLY=price_REPLACE
STRIPE_PRICE_HUSTLE_ANNUAL=price_REPLACE
STRIPE_PRICE_STACK_MONTHLY=price_REPLACE
STRIPE_PRICE_STACK_ANNUAL=price_REPLACE
STRIPE_PRICE_EMPIRE_MONTHLY=price_REPLACE
STRIPE_PRICE_EMPIRE_ANNUAL=price_REPLACE

# JWT signing secret — generate with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_RANDOM_32_CHAR_STRING

# URLs — update API_URL after Replit gives you your deployment URL
APP_URL=https://flippd.com
API_URL=https://flippd-backend.YOUR_USERNAME.repl.co
